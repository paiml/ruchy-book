// Simple test file for ruchy tooling verification
fun add(a: i32, b: i32) -> i32 {
    a + b
}

fun multiply(a: i32, b: i32) -> i32 {
    a * b
}

fun main() {
    let result = add(2, 3);
    let product = multiply(4, 5);
}