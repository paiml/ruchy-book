fun is_even(number) {
    return number % 2 == 0
}

fun describe_number(n) {
    let even_odd = if is_even(n) { "even" } else { "odd" }
    return f"{n} is {even_odd}"
}

// Test all branches for 100% coverage
println(describe_number(42))   // even
println(describe_number(43))   // odd