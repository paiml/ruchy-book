// Ch04: Control Flow - For Loop with Arrays
let fruits = ["apple", "banana", "orange"]

// Iterate through array
for fruit in fruits {
    println(f"I like {fruit}")
}

// Simple counting loop
for i in 0..3 {
    println(f"Count: {i}")
}