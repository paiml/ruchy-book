[package]
name = "dataframe-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
polars = { version = "0.44", features = ["lazy", "csv", "json", "temporal", "strings"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"