// ❌ Parameters are read-only
fn bad_function(x) {
    x = x + 1  // Error: can't modify parameter
    return x
}

// ✅ Create new variables inside functions
fn good_function(x) {
    let result = x + 1
    return result
}